#!/bin/bash
#PBS -l walltime=01:00:00 -q debug-scaling
#PBS -l filesystems=home:flare -A AuroraGPT
#PBS -N debug-scaling-smoke
#PBS -o /home/wenyiw/aurora_rayserver/tmp/scaling-smoke.stdout
#PBS -e /home/wenyiw/aurora_rayserver/tmp/scaling-smoke.stderr
# Self-contained weak-scaling smoke: runs the hardened launch_cluster + probe
# directly on the allocation. Node count comes from the -l select= at submit.
cd /home/wenyiw/exaserve
bash scripts/hardening/run_scaling_smoke.sh
echo "PBS_SCALING_JOB_DONE rc=$?"
