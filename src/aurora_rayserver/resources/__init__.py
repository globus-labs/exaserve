"""Package data for aurora-rayserver.

This subpackage holds shell scripts and other non-Python resources that
ship with the wheel. Use ``importlib.resources.files('aurora_rayserver.resources')``
to locate a resource at runtime.
"""
