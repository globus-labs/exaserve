# Benchmark suite dependencies
# Install with: pip install -r benchmarks/requirements.txt

# Stub server
starlette
uvicorn[standard]

# HTTP client (replay_client.py already requires this)
httpx[http2]

# Event loop (optional but recommended for performance)
uvloop

# Plotting and analysis
matplotlib
numpy

# Process monitoring (optional; used by bench_proxy.py for LiteLLM CPU/memory tracking)
psutil

# YAML support (already available via litellm venv, but listed for completeness)
pyyaml
