#!/bin/bash -l
#PBS -N {{JOB_NAME}}
{{PBS_MAIL_DIRECTIVES}}#PBS -l filesystems={{FILESYSTEMS}}
#PBS -A {{PROJECT}}
#PBS -k {{KEEP_OUTPUT}}
#PBS -l select={{NUM_NODES}}
#PBS -l walltime={{WALLTIME}}
#PBS -q {{QUEUE}}
#PBS -o {{PBS_STDOUT_DIR}}/
#PBS -e {{PBS_STDERR_DIR}}/

cd {{CODE_ROOT}}
source "{{ENV_SCRIPT}}"
python -m eval.cli run execute "{{RUN_YAML}}"
